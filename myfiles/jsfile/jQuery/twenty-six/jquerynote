插件: 
    封装自定义插件
    只要希望复用功能和样式时,都要封装为插件
    前提: 已经用普通的html,css,js实现了插件样式和功能
    

jquery UI:
    1.将插件的css集中定义在独立的css文件中
    2.在独立的js文件中,为jquery.fn添加一个新的插件API函数
        1.侵入: 根据插件需要为子元素自动添加和自定义扩展属性
        2.事件绑定: 
    如何使用 : 同jquery UI先引用css再引入js,再调用

bootstrap UI:
    1.将插件的css集中定义在独立的css文件中
    2.在独立的js文件中查找具有自定义扩展开属性的元素绑定事件
    3.同bootstrap   :   引入css 定义HTML内容   手动添加class和data-扩展属性   引入插件js